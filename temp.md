1. 状态降级（最推荐：视觉补位）对于没有镜像源概念的语言，不要直接留白（会显得像 Bug），而是显示一个“不可用”或“系统接管”的状态。有镜像源（如 Node）：Mirror:  🚀 npmmirror.com (12ms)无镜像源（如 Java）：Mirror:  🔒 System Default (N/A)  — 使用灰色文字，提示该语言不通过 poly-switch 管理镜像。2. 动态 Context 布局（高阶做法）右侧详情栏不应该是“死板”的固定字段，而是根据左侧选中的语言动态渲染不同的组件卡片。当选中 Node/Rust 时：右侧渲染 [状态] [版本] [镜像源] 三个区块。当选中 Java 时：右侧渲染 [状态] [版本] [JDK 类型]（比如显示是 OpenJDK 还是 Oracle JDK）。实现思路：在 Bubbletea 的 View 函数中，根据 m.selectedLanguage 使用 switch 语句分发不同的渲染逻辑。3. 底部按键的“灰度处理”这是交互体验中最重要的细节：如果当前语言没有镜像管理功能，底部的快捷键提示应该改变。选中 Node：底部显示 [M] Manage Mirrors（高亮）。选中 Java：底部显示 [M] Manage Mirrors（暗灰色/删除线）或者直接不显示该按键提示。
